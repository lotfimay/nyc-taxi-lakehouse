name: "nyc_taxi_lakehouse"
version: "1.0.0"

profile: "nyc_taxi_lakehouse"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  nyc_taxi_lakehouse:
    +materialized: table
    bronze:
      +schema: bronze
    silver:
      +schema: silver
    gold:
      +schema: gold
seeds:
  nyc_taxi_lakehouse:
    gold:
      +schema: gold
