connector.name=iceberg

iceberg.catalog.type=jdbc

# --- JDBC catalog config (PostgreSQL as catalog DB) ---
iceberg.jdbc-catalog.catalog-name=iceberg
iceberg.jdbc-catalog.connection-url=jdbc:postgresql://catalog-db:5432/iceberg
iceberg.jdbc-catalog.connection-user=iceberg
iceberg.jdbc-catalog.connection-password=iceberg
iceberg.jdbc-catalog.driver-class=org.postgresql.Driver
iceberg.jdbc-catalog.default-warehouse-dir=s3://nyc-taxi/warehouse

# --- MinIO / S3 config ---
fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.region=us-east-1
s3.path-style-access=true
s3.aws-access-key=admin
s3.aws-secret-key=password
